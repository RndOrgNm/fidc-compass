# Copy to `.env.local` for local dev (`cp .env.example .env.local`).
# Production: set VITE_REPORT_API_URL in your host/CI (same value).
# HTTP API base URL — no trailing slash (RelatorioTeste → presign / report / artifacts).

VITE_REPORT_API_URL=https://hzhhne2l9h.execute-api.us-east-1.amazonaws.com
